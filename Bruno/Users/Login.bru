meta {
  name: Login
  type: http
  seq: 3
}

post {
  url: {{URL}}api/v1/users/login
  body: json
  auth: inherit
}

body:json {
  {
      "email": "dmjendor@gmail.com",
      "password": "password123"
  }
}

tests {
  const data = res.body;
  
  bru.setEnvVar("jwt", data.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
