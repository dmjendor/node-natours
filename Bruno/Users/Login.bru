meta {
  name: Login
  type: http
  seq: 3
}

post {
  url: {{URL}}api/v1/users/login
  body: json
  auth: inherit
}

body:json {
  {
      "email": "test2@gmail.com",
      "password": "password12345"
  }
}

tests {
  const data = res.body;
  
  bru.setEnvVar("jwt", data.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
