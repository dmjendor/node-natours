meta {
  name: Delete Me
  type: http
  seq: 11
}

delete {
  url: {{URL}}api/v1/users/deleteMe
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

settings {
  encodeUrl: true
  timeout: 0
}
