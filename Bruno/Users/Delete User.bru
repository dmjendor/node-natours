meta {
  name: Delete User
  type: http
  seq: 13
}

delete {
  url: {{URL}}api/v1/users/dafu90aur
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

settings {
  encodeUrl: true
  timeout: 0
}
