meta {
  name: Delete User
  type: http
  seq: 8
}

delete {
  url: {{URL}}api/v1/users/deleteMe
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

settings {
  encodeUrl: true
  timeout: 0
}
